To submit code to USACO copy+paste code into Export without changing the class name
change date input and output to just the name of the contest .in/.out
rename export's files to the usaco contest's name
example: icyPerimeter.in/icyPerimeter.out
